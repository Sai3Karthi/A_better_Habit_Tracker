// import 'package:home_widget/home_widget.dart';
import '../models/habit.dart';

Future<void> updateWidgetData(List<Habit> habits) async {
  // Temporarily disabled - widget functionality will be re-added later
  // final data = {'habits': habits.map((h) => h.toJson()).toList()};
  // final jsonString = json.encode(data);
  // await HomeWidget.saveWidgetData<String>('habits_json', jsonString);
  // await HomeWidget.updateWidget(
  //   name: 'HabitWidgetReceiver',
  //   iOSName: 'HabitWidgetReceiver',
  // );
}
